import { NgxHidableHeaderDirective } from './ngx-hidable-header.directive';

describe('NgxHidableHeaderDirective', () => {
  it('should create an instance', () => {
    const directive = new NgxHidableHeaderDirective();
    expect(directive).toBeTruthy();
  });
});
